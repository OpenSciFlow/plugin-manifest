# Design principles

`opensciflow.yaml` should be:

- lightweight enough for tool authors to review;
- explicit about license and citation;
- clear about local/HPC execution;
- honest about hardware and runtime;
- validated before execution;
- safe by default.

The manifest does not replace upstream documentation or licenses.

